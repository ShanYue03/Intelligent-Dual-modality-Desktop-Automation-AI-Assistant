# Voice assistant mirror under Multimodal_Assistant (Python 3.11.4).
# Use the SAME interpreter you use to run main.py.

# ========== Integrated pipeline (main.py) ==========

# ASR — D:/Documents/FYP/ASR (Google SR + zh→en)
#   Original also used: openai-whisper, torch (see optional below)
SpeechRecognition>=3.8.1,<3.11
PyAudio>=0.2.13,<0.3
googletrans==4.0.0-rc1

# Router — D:/Documents/FYP/Router (HF zero-shot DeBERTa)
# torch is NOT listed here: PyPI defaults to torch+cpu (~50s/router). Install torch via
# install_router_gpu.ps1 or install_router_cpu.ps1 first, then this file.
transformers>=4.30.0,<5.0.0
# Pin below 2.x so torch/transformers wheels avoid NumPy 2 ABI warnings on Windows.
numpy>=1.21.0,<2.0

# LLM — D:/Documents/FYP/LLM/benchmark_llm.py style (Groq HTTP)
#   Original LLM venv also had: langchain, huggingface_hub, accelerate (local HF use)
requests>=2.28.0,<3
python-dotenv>=0.19.0,<2
# System layer: first search result URL (web search + open first)
duckduckgo-search>=6.0.0,<8.0.0
# Browser automation: pip install playwright; start Edge with --remote-debugging-port (see System/browser_control.py, System/Automation/)
playwright>=1.40.0,<2.0.0

# TTS — D:/Documents/FYP/TTS (Edge CLI + optional Piper binary)
#   Do NOT pip-install piper-tts on many Windows setups: it pulls piper-phonemize, which
#   often has no PyPI wheel here. Install the Piper *executable* separately and add to PATH
#   (same as your benchmark). TTS layer falls back to Edge if `piper` is missing.
# edge-tts 6.x can get WSS 403 after Microsoft API changes; use 7.2+ (works on Py 3.8 in practice).
edge-tts>=7.2.0

# System layer — screenshots + media / clipboard keys (Windows)
mss>=9.0.0,<11.0.0
pynput>=1.7.6,<2.0.0
# System layer — master volume ±N% (Windows default playback device)
pycaw>=20230407
# Clock app: focus window + type duration (pulls PyGetWindow)
pyautogui>=0.9.54,<0.10
# Optional (Linux/macOS or if piper-phonemize resolves on your machine):
# piper-tts>=1.1.0,<1.2.0

# ========== Optional: run original ASR benchmark (Whisper) ==========
# openai-whisper
# torchvision
# torchaudio

# ========== Optional: vosk ASR experiments ==========
# vosk

# ========== Optional: original TTS benchmark (gTTS + analysis stack) ==========
# gTTS>=2.3.0
# soundfile>=0.12.0
# librosa>=0.9.0,<0.11
# numpy>=1.21.0,<2.0
# scipy>=1.7.0,<1.14

# ========== Optional: local HF / LangChain style LLM bench ==========
# huggingface_hub>=0.16.0,<1.0
# accelerate>=0.20.0,<1.0
# langchain (pin per your 3.9 env if used)
