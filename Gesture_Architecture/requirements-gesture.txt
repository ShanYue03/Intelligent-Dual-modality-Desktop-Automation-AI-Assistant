# Gesture_Architecture — Python 3.11.4 (same as Multimodal_Assistant).
#
# Shared environment with Voice_Architecture:
#   Match Voice_Architecture/requirements.txt pins for numpy/pyautogui/pycaw so
#   pip does not install NumPy 2 (breaks transformers/torch stack on Windows).
#
# From D:\Documents\FYP\Multimodal_Assistant, using the SAME interpreter as Voice:
#   py -3.11 -m pip install -r Gesture_Architecture/requirements-gesture.txt
#
# If pyautogui/pycaw are already satisfied, pip leaves them unchanged.

numpy>=1.21.0,<2.0
pyautogui>=0.9.54,<0.10
pycaw>=20230407
opencv-python>=4.8,<5
mediapipe==0.10.11
protobuf>=3.20,<4
comtypes==1.1.11
screen-brightness-control==0.9.0
psutil>=5.9.0
